
@{
    ViewData["Title"] = "Reporte Excel";

    //var hoy = DateTime.Parse("01/05/2023"); // Fecha con datos
    var hoy = DateTime.Today;
    var haceUnMes = hoy.AddMonths(-1);
    var mesActual = hoy.Month;
    var yearActual = hoy.Year;
    var mesAnterior = haceUnMes.Month;
    var yearAnterior = haceUnMes.Year;
}

<partial name="_Submenu" Model="SubMenuTransacciones.Excel" />

<div class="mb-3" >
    <a asp-action="ExportarExcelPorMes" asp-route-mes="@mesActual" asp-route-year="@yearActual" class="btn btn-success" >
        Exportar mes actual
    </a> ~ @hoy.ToString("MMMM yyyy");
</div>